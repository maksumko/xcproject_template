{{#podfile}}
platform :ios, '{{deployment_target}}'

xcodeproj '../project/{{project_name}}'
workspace '../{{project_name}}.xcworkspace'

target :{{project_name}} do

end

{{#test}}
target :{{project_name}}Tests do
	{{#ocmock}}
	pod 'OCMock'
	{{/ocmock}}
end
{{/test}}
{{/podfile}}
